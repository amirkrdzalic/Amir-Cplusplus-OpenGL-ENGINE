#include "OpenGLRenderer.h"


OpenGLRenderer::OpenGLRenderer()
{
}

OpenGLRenderer::~OpenGLRenderer()
{
}

void OpenGLRenderer::renderPrimitive(PrimitiveTypeDef prim)
{
	// Load the primitive type if not already loaded
	//if (!OGLR.load(prim))
	//{
	//	OGLR.load(prim);
	//}

	//// draw the primitive type
	//OGLR.render();
}

void OpenGLRenderer::renderPrimitive(SHAPEGL * shape, Camera3D * camera, ShaderNova * program)
{
	// Load the primitive type if not already loaded
	//if (!OGLR.load(prim))
	//{
	//	OGLR.load(prim);
	//}

	//// draw the primitive type
	//OGLR.render();
}